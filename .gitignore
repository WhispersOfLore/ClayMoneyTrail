# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.vinext/
/out/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local environment
.env*

# Local-only Claude Code project instructions (operational guidance for
# assistant sessions in this repo — account-access rules, local download
# paths, etc.). Intentionally not committed to this public repo; kept
# locally only. Do not delete.
/CLAUDE.md

# deployment and generated files
.vercel
next-env.d.ts
/dist/
/dist-pages/
/.wrangler/
/outputs/
/work/
tsconfig.tsbuildinfo

# Obsidian local workspace/session state (machine-specific, not shared)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache

# Raw research scratch material (fork notes, cached source PDFs, findings.json
# drafts) for the Phase 6 expansion — kept locally for reference, not
# published. Nothing here is deleted; it just isn't tracked in the repo.
# Verified, cited facts derived from it live in data/*.json instead.
/research-staging/
